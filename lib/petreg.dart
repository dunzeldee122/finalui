import 'package:flutter/material.dart';

class PetRegistrationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pet Registration')),
      body: Center(
        child: Text('Pet Registration Page'),
      ),
    );
  }
}
